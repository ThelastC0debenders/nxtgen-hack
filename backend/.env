# SERVER CONFIGURATION
PORT=5000
JWT_SECRET=super-secret-key-for-testing

# POSTGRES (Chocolatey default values)
# If 'postgres' doesn't work as the password, you may need to use whatever you set during install.
DB_USER=postgres
DB_PASSWORD=sukz
DB_NAME=nextgen
DB_HOST=localhost
DB_PORT=5432

# REDIS (WSL default values)
REDIS_URL=redis://localhost:6379

# AI External API (Timeout in milliseconds)
AI_SERVICE_URL=http://localhost:8000
AI_TIMEOUT_MS=5000
